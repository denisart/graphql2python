{{ description }}
{%- if types|length > 1 %}
{{ name }} = Union[
{%- for type in types %}
    '{{ type }}',
{%- endfor %}
]{% else %}
{{ name }} = TypeVar('{{ name }}', bound='{{ types[0] }}'){% endif %}